<section class="auth">
    <div class="auth-form">
        <h2 class="auth-form__title">Food<span>Mania</span></h2>
        <form action="/register" method="POST">
            <% if(messages.error) { %>
                <span class="error">
                    <%= messages.error %>
                </span>
                <% } %>
                    <input type="text" name="name" value="<%= messages.name %>" placeholder="Username" />
                    <input type="email" name="email" value="<%= messages.email %>" placeholder="Email Address" />
                    <input type="password" name="password" placeholder="Password" />
                    <div class="auth-form__btn">
                        <button class="btn" type="submit">Register</button>
                    </div>
        </form>
        <div class="link">
            <span>Already have an account?</span>
            <a href="/login">Login</a>
        </div>
    </div>
    <div class="bottom-bar"></div>
</section>